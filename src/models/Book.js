class Book {
  constructor(title, author, publicationYear, copies) {
    this.title = title;
    this.author = author;
    this.publicationYear = publicationYear;
    this.copies = copies;
  }
}

export default Book;